first = input("What is your first number?")
second = input("What is your second number?")
total = first + second
total = int(first) + int(second)
if len(first) > 0 and len(second) > 0 :
    print(total)
else: print("Error: Either first or second number were not given.")
